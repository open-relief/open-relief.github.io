{
  "name": "HACKOMANIA2026",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp",
    "shared/build/dist/js/developmentLibrary"
  ],
  "scripts": {
    // top‑level helpers that just forward to the webApp workspace
    "build": "npm --prefix webApp run build",
    "start": "npm --prefix webApp run start"
  }
}